/**
 * Klass för plattformar
 * 
 */
function Platform(x, y, height, length){
    this.x = x;
    this.y = y;
    this.height = height;
    this.length = length;

    //construktor
    var img = 
    
}